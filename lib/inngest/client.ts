import { Inngest } from "inngest"

// ─────────────────────────────────────────────────────────────────────
// Inngest-клиент — durable-задачи и ретраи внешних вызовов (конституция §5):
// IMAP-поллинг, обработка вложений, OCR, LLM, распаковка архивов. Тяжёлое — НЕ
// в request-пути. Ключи — env (INNGEST_*), опц. на этапе каркаса.
// ─────────────────────────────────────────────────────────────────────
export const inngest = new Inngest({ id: "sib" })
