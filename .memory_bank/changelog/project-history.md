# Project History — хронология (вытеснено из project-state)

> Append-only летопись достижений. Снимок «где сейчас» — `project-state.md`; сюда переносится
> вытесненная хронология, чтобы снимок не раздувался. Не редактируем задним числом — только дописываем.

## Снимок «Что готово к 2026-06-21…07-21» (перенесено 2026-07-22 при HEAL под кит v1.3.0)

- ✅ **Каркас Memory Bank развёрнут** из шаблона (`memory-bank-template`), режим autopilot, тип `dev`.
- ✅ **Яндекс-почта перенесена** из sup2 + **IMAP проверен рабочим** (XOAUTH2, `imap.yandex.ru:993`, LIST OK) —
  доп. консент не нужен (ADR D2). Креды — `_secrets/ACCESS.md`, рантайм — `.env.local`.
- ✅ **Стек зафиксирован** (ADR D1): Next.js + TS + Tailwind/shadcn + Postgres/Drizzle + Inngest.
- ✅ **Инженерная конституция принята** (ADR D3) → `.claude/rules/engineering-principles.md`.
- ✅ **Каркас Next.js-приложения собран** (план `infra-start-bootstrap`): Next 16/React 19/TS строгий/
  Tailwind 4/shadcn (radix, токены WFM)/Drizzle/Zod/vitest/exceljs; ленивый db-клиент, `/api/health`,
  Dockerfile с гейтом typecheck+test+build. **Зелёный гейт** typecheck+lint+test+build.
- ✅ **GitHub + CI/CD + прод** (ADR D8): репо `igortsk123/sib`, push→авто-деплой (systemd-таймер),
  контейнер `sib-frontend` за https://sib.docon.pro (health=200), отдельный `sib-db`, авто-очистка диска.
  Всё **изолировано от sup2**. UI-базлайн = WFM shadcn (ADR D9).
- ✅ tmux-автосессия `igor` при открытии воркспейса (`.vscode/settings.json`).
- ✅ **Разобран тестовый корпус** (51 письмо / 13 страховых): структура пересылки/base64, реестр доменов,
  матрица форматов — `domain/insurer-recognition.md`. Парсинг-стратегия — ADR D10 (детерм. ядро + LLM на поля).
- ✅ **Phase 0 распознавания готов** (деплой `fa5d6e2`): схема БД (7 таблиц, EmailMessage 1—N GuaranteeLetter),
  реестр 13 страховых засеян, LLM-клиент (OpenAI прокси, gpt-5.4-mini) + Inngest подключены. Гейт зелёный.
- ✅ **Админ-панель готова** (деплой `9ee222b`, ADR D11): вход через Telegram (`doconpro_bot`, телефон→код),
  мультитенантность (платформенный админ → клиники; владелец клиники → сотрудники), адаптив (моб+десктоп) в
  стиле WFM, RBAC-гарды. Страницы: /login, /admin/clinics, /staff, /insurers (13 страховых), /registry (empty).
  12 таблиц на проде, Telegram-поллер активен. Bootstrap-админ: `+79234097976`.
- ✅ **Контроль доступа готов** (деплой `d0f2ba3`): **закрытый вход** (код/сессия только заведённым телефонам
  + bootstrap-админ; бот не пускает чужих — медданные), фикс logout, страница клиники (назначение админа
  клиники + сотрудники), управление страховыми/доменами платформенным админом. Роль owner = «Администратор клиники».
- ✅ **Демо-реестр ГП готов** (деплой `e519d99`, ADR D12): 51 письмо корпуса → **55 записей** на проде.
  Гибрид извлечения (таблицы по заголовкам / свободный текст → LLM gpt-5.4-mini / сканы → vision) +
  confidence + **пометка «Проверить»** (34 записи) в реестре/карточке/Excel. Поиск, оригиналы на сервере
  (`/api/original`), кликабельные ссылки для сверки, выгрузка Excel. OpenAI подключён (из sup2).
- ✅ **Точность реестра поднята** (ADR D13): фикс раскрутки (ГП во вложении — PDF больше не терялись;
  54 вложения), обработка по типу (PDF/RTF/XLSX/ZIP), промпт «пациент≠директор». Покрытие: ФИО 35/55,
  полис 27/55, даты 43/55; needsReview 26/55. Читаемый просмотр письма (mailparser). Стратегия по
  страховым — `domain/insurer-recognition.md`.
- ✅ **Реестр доведён до точности** (ADR D14): .xls через xlrd, ZIP-расшифровка+объединение источников,
  ФИО Титульно. Покрытие ФИО **46/50 (92%)**, needsReview 10/50. Карточка = единая сверка (все источники iframe).
- ✅ **Мультитенант-скоуп реестра**: записи привязаны к клинике (organizationId, миграция 0003);
  сотрудник видит свою клинику, платформенный админ — **переключатель клиники** (cookie) / «Все клиники».
  Корпус (50) → «Клиника Сибирская».
- ✅ **100% разбор корпуса** (деплой `2bdd5ae`): статусы **открепить/прикрепить** + согласовано/отказ;
  списки прикрепления/открепления построчно; RTF (striprtf), бинарный .xls (xlrd), ZIP-расшифровка.
  70 записей, ФИО 69/70, needsReview 1. **Чёткий алгоритм** распознавания — `domain/recognition-architecture.md`.
- ✅ **Точность распознавания + многоблок/маски** (деплой `4def319`): ВЕРХ-регистр ФИО, ФИО из темы письма
  (Совкомбанк), английские заголовки Excel (Альфа), разрыв строк, многоблочные письма, маски полисов
  по страховым, .doc (olefile). Корпус: 61 запись, ФИО 61/61, полис 60/61.
- ✅ **Расширение полей + гибрид-лог (ADR D15, прод `243a99a`)** — *работа 21.06, в память внесена 22.06*:
  новые поля (дата рожд [ФИКС бага], № договора, № обращения, период обслуживания, **срок действия письма**,
  ограничение-сумма/условия, **docType**, серия полиса); статус **«аннулировать»**; **гибрид
  «детерминизм→LLM-фолбэк»** с логом пробелов парсера → таблица **`parse_log`** (миграция 0008) и страница
  админки **`/parse-log`** (ловить смену форм источником). Полный Excel-экспорт (все поля + ссылка на карточку
  + автофильтры; устаревшие флаги убраны). Миграции **0005–0008**. Модели: gpt-5.5 (обучение) / gpt-5.4-mini (рантайм).
- ✅ **Понятные подсказки «проверить»**: `lib/review-hints.ts` — техническое имя поля → человеческая
  формулировка в карточке/тултипе (вместо «Причина: patientBirthDate»). Статус annul в фильтре реестра.
- ✅ **Админка шаблонов типов документов** (деплой `995bdee`, MVP): `/insurers/[id]` → секция «Типы
  документов» — таблица `doc_template` (миграция 0009), загрузка образца + текст, «Эталон LLM» (gpt-5.5)
  → gold_json, счётчик «N на разбор» по типу из `parse_log` (+ `doc_type`). RBAC платформенного админа.
  v2 (после S1): авто-сборка парсера из эталона, live-переразбор. План — `completed_plans/admin-doctype-templates.md`.
- ✅ **Официальные наименования страховых** (деплой `4d4f4cf`): `insurance_company.name` → офиц. (ПАО СК
  «Росгосстрах», АО «СОГАЗ», … ООО «Балт Ассистанс»), короткие имена корпуса → в `aliases`. Видны везде
  (реестр/карточка/фильтр/Excel — читают name из БД). Сид идемпотентен по ДОМЕНУ (run.ts): переименование
  без дублей (13 строк). Корпус матчит по name+aliases.
- ✅ **Деление амбулатория/стоматология** (careType, миграция 0010): поле `care_type` (классификатор
  `lib/care-type.ts` по услугам/тексту), **фильтр «Направление»** в реестре И в Excel, колонка «Направление»
  (реестр/карточка/Excel). Bulk-экспорт под стомат-импорт — ждёт формат их программы (v2).
- ✅ **Ревизия веб-таблицы реестра**: колонка «Проверка» — первой, жёлтой иконкой (без текста) + **фильтр
  по проверке**; «Страховая» сокращается с тултипом (hover/тап — `Truncate`); период обслуживания в Excel —
  две колонки (дата начала/окончания); таблица min-w + горизонтальный скролл.
- ✅ **Формат дат ДД.ММ.ГГГГ везде** (`14613d9`): `lib/format.ts` `ruDate()` в реестре/карточке/Excel (было ISO).
  Тултип страховой — подсказка при наведении/тапе (не раскрывает строку). Фикс ширины таблицы реестра
  (убрана двойная обёртка overflow → корректный горизонтальный скролл).
- ✅ **Фильтры дат** (`cf1c5ac`→текстовые дд.мм.гггг): нативный date показывал локаль-формат (ММ/ДД/ГГГГ) и не
  валидировал → заменены на текстовые поля с маской дд.мм.гггг + валидация (день 01-31, мес 01-12, год 1900-2099),
  конвертация в ISO для запроса/Excel (`isoFromRu`). Нельзя ввести мес 15 / год 0001.
- ✅ **«Сообщить об ошибке» в карточке** (миграция 0011): кнопка в карточке → диалог (свободный текст + опц.
  почта) → таблица `error_report`. Админ-страница `/error-reports` (платформенный админ, в навигации):
  список, ссылка на запись, «Исправлено»/«Не ошибка». При «Исправлено» + почте — письмо автору
  («спасибо, ошибка <ссылка> исправлена, постараемся больше не допускать»). **SMTP-отправки в приложении
  пока нет** → письмо логируется как pending (`notifyErrorFixed`), реальная отправка — после поднятия SMTP.
- 🛠 **Инцидент диска (решён)**: build cache дорос до 17GB → диск 100% → деплой падал «no space left», прод 503.
  Освобождён `docker builder prune` (100%→68%). Деплой-скрипт в репо теперь **ограничивает кэш**
  (`docker builder prune --keep-storage 3GB`). ✅ Серверная копия `/usr/local/bin/sib-deploy.sh` обновлена
  (с разрешения владельца, бэкап `.bak.*`); кэш сброшен до 3GB, диск 69%. Будущие деплои держат кэш ≤3GB.
- ✅ **Email-связка репортов**: «Сообщить об ошибке» больше НЕ спрашивает почту — берёт из аккаунта
  (`appUser.email`). Добавление сотрудника (`addStaff`/диалог) теперь принимает **почту** (необязательно, с
  пояснением: на неё уведомления — много писем/срочное/исправления).
- ✅ **careType «Комплексное» (combined)** (`c78c99b`, миграция 0013) + **классификация LLM по УСЛУГЕ** (`db7f2ae`):
  направление ставит LLM — по услуге в первую очередь (стоматологическая услуга → стоматология, даже под
  амбулаторной программой; напр. «Снятие зубных отложений» → стоматология, НЕ combined); нет услуги → по смыслу;
  combined только если реально оба (напр. «АМБУЛАТОРНАЯ+СТОМАТОЛОГИЧЕСКАЯ ПОМОЩЬ»). classifyCareType — фолбэк
  по услуге для строк-реестров (не по ко-встречаемости слов). Корпус: амбулатория 34 · стоматология 19 · комплексное 8.
- ✅ **Статус-поле в карточке** + метод русскими метками; **«скачать оригинал»** вместо iframe (`c78c99b`).
- ✅ **Правка почты сотрудника** (`a678caf`): инлайн в /staff и /admin/clinics/[id] (платформенный админ
  или владелец); почта при добавлении и редактировании.
- ⏭ **Очередь — SMTP-отправка** (Яндекс XOAUTH2): отправка наружу (smtp.yandex.ru) — нужна проверка
  коннективности + рабочий OAuth-токен (outward-facing) → делать с владельцем. Пока письма логируются (pending).
- ✅ **Журнал распознавания по-русски** (`5fd43c7`): методы (Парсер/Парсер+ИИ/ИИ — `METHOD_LABELS`), поля
  (`FIELD_HINTS`), блоки «Зачем»/«Как пользоваться», «Что добирал ИИ». Excel «Метод» — те же метки.
- ✅ **Журнал — в контексте ШАБЛОНА** (`efcd667`→`09d6c9f`, план per-template-parse-journal): страховая→домен→
  шаблоны→журнал по каждому шаблону. Предзаполнены **27 шаблонов** с ОБРАЗЦОМ (тема+тело+файл из корпуса,
  миграция 0012 `sample_subject`) — есть что гнать через «Эталон LLM». В карточке: тема образца / файл / есть
  текст + журнал (записей/чем разобрано/что добирал ИИ). ФИКС: parse_log хранит ОФИЦИАЛЬНОЕ имя → журнал не
  пустой (короткое имя не совпадало после переименования). /parse-log упрощён до глобальной сводки.
  Фикс бага: отмена prompt/confirm в репортах больше не помечает (`5e75b3a`). Маска дат дд.мм.гггг (DateMaskInput).
  Логика (владелец): новый тип = пример письма (тема+тело+файл) → итеративно через LLM (parsingHints) → парсер.
- ✅ **Страница шаблонов = управление** (`752337b`): тема/тело образца РЕДАКТИРУЕМЫ (SampleEditor →
  updateDocTemplateSample); убраны кнопка «Эталон ИИ» и вывод gold-JSON (эталон — инструмент разработчика,
  не для пользователя/админа), убран бейдж «N на разбор» (дублировал журнал). Осталось: добавить/удалить тип,
  файл-образец, журнал по шаблону. Действие `extractGold` остаётся в коде как dev-инструмент (не в UI).
