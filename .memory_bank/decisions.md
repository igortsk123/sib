---
tier: 1
topic: decisions
scope: ADR-лог — архитектурные решения с обоснованием и влиянием
tier2: ""
updated: 2026-07-23
importance: high
source: manual
status: stable
source_of_truth: canonical
last_verified: 2026-06-21
review_after: ""
---

# Decisions — ADR-лог

Хронологический лог архитектурных решений. Новое решение, отменяющее старое, не удаляет старую запись,
а добавляется ниже со ссылкой «отменяет [дата]». Формат: **Решение / Почему / Альтернативы / Влияет на.**

---

## [2026-06-21] D1. Стек — Next.js + TypeScript (full-stack), Drizzle/Postgres, Inngest
**Решение:** один TypeScript-моно-стек: Next.js (App Router) + TS (строго, без `any`) + Tailwind/shadcn
для UI; PostgreSQL + Drizzle ORM; **Inngest** для durable-задач (IMAP-поллинг, обработка вложений, OCR,
LLM) и ретраев внешних вызовов. LLM — OpenAI через RU-прокси (креды доступны из инфры владельца).
**Почему:** владелец задал «TypeScript Next.js полностью, чтобы ловить» (ошибки на компиляции); тот же
владелец/инфра, что и sup2 → переиспользуем паттерн Яндекс-почты XOAUTH2; Inngest закрывает принцип
«внешний вызов всегда в обёртке с ретраем» и выносит тяжёлую обработку из request-пути.
**Альтернативы:** отдельный Python-бэкенд для OCR/LLM — отвергнут (два стека усложняют самопроверку и
единый гейт; тяжёлые обработчики при необходимости вынесем позже как сервис — это **гипотеза**, проверим
на реальных объёмах). App Router vs Pages — App Router (актуальный, server actions).
**Влияет на:** всю структуру, `core/architecture.md`, `.claude/rules/code-standards.md`, `quality-criteria.md`.

## [2026-06-21] D2. Почта — входящая через Яндекс IMAP XOAUTH2; доступ подтверждён эмпирически
**Решение:** письма забираем по `imap.yandex.ru:993` (SSL, XOAUTH2) тем же OAuth-приложением и
refresh-токеном, что и sup2; ящик `palmarius.ru@yandex.ru` (тестовый). Отправка (если понадобится) —
`smtp.yandex.ru:465` тем же токеном.
**Почему:** бриф §3/§14 — продукт **забирает входящие** письма; владелец предполагал, что доступ уже есть.
**Проверка (гипотеза→факт, принцип «проверь»):** 2026-06-21 XOAUTH2-логин на IMAP = `OK`, `LIST` папок
работает — **хотя** в sup2 scope задокументирован как `mail:smtp`. Значит токен фактически покрывает IMAP,
браузерный консент НЕ требуется.
**Альтернативы:** перевыпуск токена со scope `mail:imap` (не понадобился); app-password (XOAUTH2 лучше —
не светит пароль ящика). На рабочем этапе — 4 ящика клиники (могут быть не-Яндекс) → `core/human-decisions.md`.
**Влияет на:** `core/email-ingestion.md`, `_secrets/ACCESS.md`, `.env.*`, `sync-with-external.md`.

## [2026-06-21] D3. Инженерная конституция — самопроверяемость
**Решение:** так как владелец **не ревьюит код построчно**, работа должна быть самопроверяемой:
вертикальные срезы UI→БД с unit (чистая логика) + e2e (критический поток); полный гейт
(typecheck+lint+unit+integration+e2e+build) перед словом «готово»; внешние вызовы — в обёртке с
Inngest-ретраями и заданным user-facing состоянием ошибки; спрашивать владельца только по
`core/human-decisions.md`; любое отклонение от спеки — ADR.
**Почему:** без построчного ревью качество держится на автоматических проверках и явных решениях.
**Альтернативы:** доверять ручной проверке — отвергнуто (владелец её не делает).
**Влияет на:** `.claude/rules/engineering-principles.md`, `quality-criteria.md`, весь процесс разработки.

## [2026-06-21] D4. ПДн и медицинская тайна — повышенная безопасность
**Решение:** проектируем как продукт повышенной чувствительности: хэш паролей пользователей,
шифрование токенов почтовых ящиков, RBAC по ролям, аудит-лог действий, ограничение массовой выгрузки
без прав, HTTPS, доступ к оригиналам писем/вложений по правам, бэкапы, возможность обезличить/удалить
по регламенту. Место хранения ПДн, оператор данных и требования клиники — **решение владельца**.
**Почему:** письма содержат персональные и медицинские данные пациентов (бриф §16).
**Альтернативы:** отложить безопасность на потом — отвергнуто (ПДн с первого среза).
**Влияет на:** `.claude/rules/guardrails.md`, `core/roles-and-access.md`, `core/data-model.md` (AuditLog), `deployment.md`.

## [2026-06-21] D5. Двойная пересылка — первоклассный кейс, не частный
**Решение:** для каждого письма храним и технические данные **последнего** письма, и извлечённые данные
**исходного** (реальный отправитель, исходные дата/тема, реальные вложения, вложенные `.eml`/`message/rfc822`).
Логика разбора пересылки (`Forwarded message` / `Пересылаемое сообщение` / `От кого`…) остаётся резервным
механизмом и на рабочем этапе.
**Почему:** тестовые письма дважды пересланы (клиника→разработчик→тестовый ящик), §3/§18.1 — иначе
извлечём данные пересылки вместо данных страховой.
**Влияет на:** `core/email-ingestion.md`, `core/recognition.md`, `core/data-model.md` (EmailMessage: forwarded-поля).

## [2026-06-21] D6. Дедупликация — помечать, не удалять автоматически
**Решение:** возможные дубли определяем по сигналам (Message-ID исходного письма, hash вложения, номер
гарантийного письма, пациент+страховая+дата+услуги, совпадение исходного текста) и **помечаем** в
админке; автоматически безвозвратно не удаляем.
**Почему:** письма пересылаются и повторяются; авто-удаление рискует потерять данные (бриф §12).
**Влияет на:** `core/recognition.md`, `core/admin-panel.md`, `core/data-model.md`.

## [2026-06-21] D7. Типизация — простые типы + явные DTO + Zod на границах; минимум «магии»
**Решение:** простая, читаемая типизация: понятные **DTO** (явные `type`/`interface`), **Zod-схемы** на всех
границах (входящие письма/вложения, ответы OpenAI, формы, env, payload'ы Inngest; TS-типы — через `z.infer`,
для БД — `drizzle-zod`). **Минимум «магии»**, без сверхсложных generic-типов без необходимости. Клиент↔сервер —
через **Next.js App Router (Server Actions / Route Handlers)** с явными типизированными DTO; **tRPC по умолчанию НЕ вводим**.
**Почему:** владелец задал стиль — «простые типы, понятные DTO, Zod-схемы, минимум магии, никаких сверхсложных
generic без необходимости». Для внутренней админки (немного пользователей, §19.4) Server Actions + Drizzle-инференс
+ Zod дают сквозную типобезопасность без лишнего слоя; тяжёлые generic/RPC-абстракции труднее читать и держать в
голове без построчного ревью (противоречит самопроверяемости, D3).
**Альтернативы:** **tRPC** — отложен. **Отложенный триггер** (`quality-criteria.md`): ввести при появлении
(а) отдельного клиента (mobile/RN/внешний SPA), (б) публичного типизированного API для клиники (§19.5), или
(в) боли от бойлерплейта Server Actions на реестре/очереди. Zod вводим сразу.
**Влияет на:** `.claude/rules/code-standards.md`, `core/architecture.md`, `core/data-model.md`, `sync-with-external.md`, `quality-criteria.md`.

## [2026-06-21] D8. Инфра/CI-CD — методология sup2, но полная изоляция
**Решение:** репо `igortsk123/sib` (приватный); деплой — **push в `main`** → серверный systemd-таймер
`sib-deploy.timer` (~2 мин) гонит `sib-deploy.sh`: tag prev → docker build (**гейт typecheck+test в
Dockerfile**) → `db:migrate` → swap → smoke `/api/health` → rollback на `sib:prev`. Прод на том же VPS
`193.160.208.41`, что sup2, но **всё своё**: deploy-ключ `sib_deploy`/алиас `github-sib`, сеть `sib-net`,
БД `sib-db` (:5434), контейнер `sib-frontend` (:3006), `/opt/sib`, `/opt/sib.env`, nginx `sib.docon.pro`
+ TLS. Авто-очистка диска (image+builder prune после деплоя + еженедельный cron, без `-a` на образах).
GitHub Actions НЕ заводим — таймера достаточно (проще, меньше секретов).
**Почему:** владелец просил «CI/CD как в sup2», но «максимально независимо». Методология sup2 проверена на
этом сервере; изоляция ресурсов даёт независимость без переизобретения (sup2 можно удалить — sib живёт).
**Альтернативы:** GitHub Actions instant-trigger (как у sup2) — отложен (лишние секреты на сервер);
общий с sup2 Postgres — отвергнут (изоляция ПДн, ADR D4). Cloudflare сейчас DNS-only → HTTP-01 прошёл.
**Влияет на:** `deployment.md`, `_secrets/ACCESS.md`, `project-state.md`, `deploy/` в репо.

## [2026-06-21] D9. UI-базлайн — стандартный shadcn (radix/new-york) из WFM-admin, не из sup2
**Решение:** UI-фундамент берём из **WFM-admin** (донор админки): дизайн-токены `app/globals.css` (OKLCH,
бренд-виолет, семантические токены, badge-схемы, sidebar/chart-токены) + примитивы shadcn на **radix**
(style `new-york`). Тулчейн (Next 16/React 19/Tailwind 4/Drizzle/Zod/vitest/exceljs, Dockerfile, health) —
по образцу sup2.
**Почему:** sup2 использует shadcn `base-nova` на `@base-ui` — несовместим с radix-примитивами WFM «в лоб».
Брать UI из WFM и одновременно равняться на base-ui sup2 = лишняя адаптация и привязка к sup2. Стандартный
radix-shadcn WFM переносится напрямую и независим от sup2.
**Альтернативы:** mirror base-nova sup2 + адаптация WFM под base-ui — отвергнут (дороже, привязка к sup2).
**Влияет на:** `components.json`, `app/globals.css`, `components/ui/*`, `.claude/rules/ui-rules.md`, `core/admin-panel.md`.

## [2026-06-21] D10. Парсинг — детерминированное ядро + LLM-извлечение полей (по эмпирике корпуса)
**Контекст (факты с реального теста, `domain/insurer-recognition.md`):** корпус = контейнер от
`igortsk@mail.ru` → 13 `message/rfc822` (страховые) → 51 письмо, каждое = **оригинал в base64 внутри
`text/plain` БЕЗ `Content-Transfer-Encoding`** (станд. парсер не раскрывает); часто `From` оригинала
**срезан** пересылкой. Все 13 доменов найдены (ingos.ru, sogaz.ru, reso.ru, vsk.ru, rgs.ru, alfastrah.ru,
zettains.ru, sovcomins.ru, renins.com, calltravel.eu=Балта, luchi.ru=Лучшее, astrovolga.ru, energogarant.ru).
**Решение:**
- **Детерминированно (без LLM, покрыто unit на корпусе):** (1) рекурсивная раскрутка пересылки —
  `message/rfc822` + **детект-и-декод base64-письма в text/plain даже без CTE** → до оригинала;
  (2) идентификация страховой по **реестру доменов** (`InsuranceCompany.domains[]`, сид = 13 выше) с
  фолбэком на тело/Received (НЕ «любой внешний домен» — в теле домены клиник); (3) извлечение текста по
  форматам (HTML→текст, PDF-текст/OCR, DOC/DOCX, RTF, **XLS/XLSX через exceljs**, ZIP±пароль, изображения→OCR);
  (4) дедуп, валидация полноты, очередь ручной проверки.
- **LLM (OpenAI через прокси, Structured Outputs `strict`)** — только **извлечение структурированных полей
  ГП** из уже очищенного текста (где правила хрупки, §18.2). Клиент — порт паттерна sup2 `lib/server/llm/openai.ts`
  (`OPENAI_BASE_URL`→`/chat/completions`, модель **`gpt-5.4-mini`**, прокси РФ обязателен). LLM ещё — фолбэк
  идентификации страховой при промахе реестра.
**Почему:** структуру нельзя угадывать (ошибёмся) → детерминированное ядро, проверяемое на 51 реальном письме;
вариативность шаблонов полей лучше закрывает LLM с гарантированной JSON-схемой, чем регэкспы. Владелец
разрешил использовать OpenAI по API на этих данных.
**Логирование (решение владельца 2026-06-21):** полное логирование РАЗРЕШЕНО, включая тело запросов/ответов
LLM и распознанный текст — **целевой хостинг = собственный РФ-сервер мед.клиники** (данные под контролем
клиники, не покидают её контур, кроме явного вызова OpenAI-прокси). Можно переиспользовать лог sup2 как есть.
В логи НЕ пишем только **секреты** (OAuth/OpenAI-токены, пароли архивов) — это гигиена, не ПДн. В прокси
OpenAI уходит необходимый текст письма (внешний вызов, разрешён владельцем).
**Альтернативы:** только-регэкспы/правила — отвергнуто (хрупко на вариативных шаблонах); только-LLM на сыром
письме — отвергнуто (LLM захлебнётся в base64/пересылке и наделает ошибок; сначала детерминированно очищаем).
**Влияет на:** `core/recognition.md`, `domain/insurer-recognition.md`, `core/data-model.md` (InsuranceCompany.domains),
`plans/recognition-roadmap.md`, `_secrets/ACCESS.md` (OpenAI), `.claude/rules/guardrails.md` (PII в логах LLM).

## [2026-06-21] D11. Вход через Telegram + мультитенантность (порт sup2)
**Контекст:** владельцу нужна админка для показа результата клинике (IMAP отложен): вход как в sup2 (отдельный
бот `doconpro_bot`), платформенный админ заводит клиники, владелец клиники — сотрудников.
**Решение:**
- **Вход:** телефон → код в Telegram-боте → сессия (cookie `sib_session`, 30 дн). Хостер режет Telegram →
  **long-poll через прокси** `TELEGRAM_API_BASE` (тот же, что sup2: `tg.claude-access.ru`), старт из
  `instrumentation.ts`. Таблицы `session/login_attempt/telegram_contact`. Тест-вход (фикс. телефон+код) для демо.
- **Мультитенантность:** `organization`(клиника) + `membership`(user↔клиника+роль) — паттерн sup2. `app_user`
  переведён на **телефон** (ключ входа) + `is_platform_admin` (платформенный админ — глобально, вне клиники).
  Роль в клинике — `membership.role` (owner/dms/doctor/registry/registry_senior). Bootstrap первого
  платформенного админа по `BOOTSTRAP_ADMIN_PHONE`.
- **RBAC:** гарды на каждый server action (`requireUser`/`requirePlatformAdmin`/`requireClinicOwner`) — не
  только на отрисовку (server action = публичный POST).
- **UI:** адаптивная оболочка (сайдбар+топбар+мобильная нижняя навигация) в стиле WFM на портированных токенах.
**Почему:** проверенный паттерн sup2; мультитенант через membership гибче, чем org на пользователе.
**Схема — гипотеза, данных нет:** миграции пересозданы одним файлом, схема sib-db сброшена (без потерь).
**Влияет на:** `core/roles-and-access.md`, `core/data-model.md`, `_secrets/ACCESS.md` (бот/прокси), `instrumentation.ts`, `plans/admin-panel.md`.

## [2026-06-21] D12. Извлечение полей — гибрид по типу документа + пометка проверки (точность)
**Контекст:** демо рабочего реестра из корпуса (51 письмо → 55 записей). Владелец: «текст извлекай сам,
фото — LLM; максимально гарантировать точность; помечай низкую уверенность для перепроверки».
**Решение (по типу документа):**
- **Чистые таблицы** (Excel-реестр, напр. Альфа «Список застрахованных») — **детерминированно по заголовку**:
  ищем строку-шапку, сопоставляем поле↔колонку по ключевым словам, читаем ячейки. Точно, без LLM.
- **Свободный текст** (тело, текстовый PDF, письмо-в-Excel у РЕСО) — **LLM** (gpt-5.4-mini через прокси,
  текстовый режим): отдаём очищенный текст → семантический разбор в JSON-поля. Регэкспы тут ненадёжны (свободная вёрстка).
- **Сканы/изображения** — **LLM-vision** (PDF без текстового слоя → png → vision).
- **Точность:** LLM возвращает confidence; **кросс-чек** с детерминированным; «точность важнее полноты»
  (сомнение → null). **`needsReview`** (нет ключевых полей / низкая уверенность / расхождение) → пометка
  «Проверить» в реестре, карточке и **отдельной колонкой в Excel** + кликабельная ссылка на оригинал для сверки глазами.
**Эмпирика-правка:** РЕСО-xlsx оказался ОДНИМ письмом в Excel (не реестр) — убран из реестров; реестр —
только Альфа. Покрытие после LLM: ФИО 58%, полис 31%, № ГП 42% (остальное — null с пометкой, честно).
**Хранение:** оригиналы (.eml+вложения) на сервере (`/opt/sib-storage`→`/app/storage`), раздача `/api/original`
по сессии (ПДн). Это и есть инвариант «ссылка на исходник всегда».
**Влияет на:** `domain/insurer-recognition.md`, `core/data-model.md` (guarantee_letter: method/confidence/needsReview),
`lib/server/llm/openai.ts` (фикс прокси), `_secrets/ACCESS.md` (OpenAI), `plans/registry-demo.md`.

## [2026-06-21] D13. ГП часто во вложении; раскрутка обязана сохранять вложения; стратегия по типу
**Контекст:** на демо часть записей оказались пустыми. Причина — **баг раскрутки пересылки**: при спуске в
`multipart/alternative` (тело html) терялся соседний `application/pdf` — само гарантийное письмо. Тело многих
писем — лишь уведомление; данные в PDF/Excel-вложении.
**Решение:** раскрутка спускается ТОЛЬКО через обёртки (`message/rfc822`, base64-письмо в text/plain), а любой
реальный multipart возвращает ЦЕЛИКОМ — со всеми вложениями. После фикса: 54 вложения (было 33), покрытие ФИО
35/55, полис 27/55 (было 17). Обработка по типу: PDF→pdftotext (скан→vision), RTF→стриппер, XLSX→таблица/текст,
ZIP→unzip паролем из соседнего письма. PNG в письмах — логотипы (не ГП). Промпт LLM: **пациент ≠ директор/адресат**
(был баг — брал «Директору Слуцкой Д.Л.»). Полная матрица — `domain/insurer-recognition.md`.
**Остаётся (follow-up):** DOC и бинарный .xls (нужен libreoffice/catdoc на сервере); ZIP с AES/per-архивным
паролем (Зетта/Росгосстрах — частично); vision на реальные сканы.
**Влияет на:** `.mail-intake/extract_dataset.py`+`enrich.py` (офлайн-пайплайн), `domain/insurer-recognition.md`, `plans/registry-demo.md`.

## [2026-06-21] D14. Объединение источников в одну карточку; ZipCrypto-расшифровка; ФИО Титульно
**Решение:**
- **Одна запись пациента = один набор источников** (`guarantee_letter.sourceEmailIds`): письмо ГП +
  связанные **письма-пароли** (по № ГП). Письма-пароли — НЕ отдельные записи (Зетта 6→3, Росгосстрах 5→3).
  Карточка показывает все источники **подряд, встроенными iframe** (письмо страховой + письмо-пароль +
  расшифрованный PDF) — единая сверка глазами по одной ссылке (требование владельца).
- **ZipCrypto-архивы** расшифровываются паролем из письма-пароля (Python zipfile; пароли со спецсимволами
  `:;%?}*` — нельзя через shell-`unzip`). Расшифрованный PDF/xls сохраняется и парсится. Зетта 3/3 ФИО.
  AES-архивы — пока нет (нужен pyzipper) → пометка «архив без пароля».
- **ФИО** к виду «Фамилия Имя Отчество** (нормализуем полностью ВЕРХНИЙ регистр; смешанный не трогаем).
- **Просмотр письма** — читаемый рендер (mailparser), не сырой .eml.
**Покрытие после фиксов:** ФИО 37/50 (74%), полис 29/50, даты 42/50, needsReview 19/50.
**Влияет на:** `guarantee_letter.sourceEmailIds` (миграция 0002), `app/(admin)/registry/[id]` (iframe-сверка),
`lib/db/seed/corpus.ts`, `.mail-intake/*`, `domain/insurer-recognition.md`.

## [2026-06-22] D15. Разбор в контексте ФАЙЛА (не человека) + гибрид «детерминизм→LLM-фолбэк»+лог + новые поля
> Зафиксировано постфактум: работа сделана 21.06 (коммиты `82f16cb`→`243a99a`), но в память не попала
> (соседний чат вёл sib из контекста sup2). Прод на `243a99a`, 61 запись. Сверено по корпусу.
**Контекст:** MVP-поля (ФИО/полис/№ГП/статус) расширены под реальные требования владельца и формы страховых.
**Решения:**
- **Контекст ФАЙЛА, не человека** (принцип владельца): реестры однотипны — настраиваем колонки/правила по
  ОДНОЙ строке файла × страховой, и тогда **все люди реестра парсятся тем же правилом детерминированно** (без
  LLM на каждого). Прогон/отладка — на уровне уникальных типов документов в разрезе страховой (у некоторых 2-3 типа).
- **Гибрид «детерминизм-первой → LLM-фолбэк» + ЛОГ** (`enrich.py`): каждое поле сначала детерминированно; чего
  парсер не дал — добирает LLM (`method="deterministic+llm"`). Поля, что **LLM нашла, а парсер НЕТ** (`detGap`),
  пишутся в `parse_log.jsonl` → таблица **`parse_log`** (миграция 0008) → страница админки **`/parse-log`**.
  Зачем: ловить **смену форм источником** — поле «уезжает» в LLM → видно в журнале → донастраиваем правило.
- **LLM-самопроверка/обучение:** сильная модель **gpt-5.5** (`ENRICH_MODEL`) на файл-эталон (просим вернуть
  максимально полный СТРОГИЙ JSON без перефраза) → по нему настраиваем regex/колонки. **gpt-5.4-mini** — рантайм-фолбэк.
- **Новые поля GuaranteeLetter** (миграции 0005–0007): `patientBirthDate` (ФИКС бага — писалась дата письма),
  `contractNumber` (№ договора, напр. F514Z/045/7393S/24), `caseNumber` (№ обращения/направления),
  `policySeries`, `coverageFrom`/`coverageTo` (период обслуживания), `validUntil` (срок действия письма —
  Зетта: до какой даты страховая оплачивает), `amountLimit`+`conditions` (ограничения), `docType` (enum),
  `reviewStatus`, `insurerComment`/`clinicComment`. См. `core/data-model.md`.
- **Новый статус `annul` (Аннулировать)** ≠ `detach` (Открепить): часть страховых (Зетта/Совкомбанк) просят
  именно аннулировать ГП. Тип документа `docType`: guarantee/enroll/detach/annul/referral(направление)/…
- **Excel-экспорт пересмотрен:** все поля + ссылка на карточку («всё в одном месте») + автофильтры + фикс-шапка.
  **Устаревшие флаги «требует проверки»/жёлтая подсветка убраны из Excel.**
- **Пометка «проверить» — понятным языком** (`lib/review-hints.ts`): техническое `reviewNote` («patientBirthDate»)
  маппится в человеческую подсказку в карточке/тултипе. Замена «Причина: patientBirthDate» (непонятно владельцу).
**Бизнес-правила по страховым (валидированы владельцем на корпусе):**
- **Альфа:** снятие/постановка = Excel (вкл. технический `*_all.xlsx` с en-колонками) + ГП в RTF; № договора в Excel.
- **Астро-Волга:** «направление в пределах суммы» → `amountLimit`/`conditions` (где сумма, где манипуляции).
- **Балта:** ограничение в PDF «доп. информация» («Покрываем: …; ВАЖНО! …») → `conditions`.
- **Зетта:** `validUntil` (срок действия письма); статус `annul`. **СОГАЗ:** чёткие поля в Excel — разбор по колонкам.
- **Ренессанс:** ФИКС склейки полиса — № обращения (192/11430) и код программы больше НЕ приклеиваются к полису
  (чистится по маске `00\dДМС…/…`, № обращения → `caseNumber`).
**Покрытие на проде (61 запись):** ФИО 61/61, полис 60/61, №ГП 49–51, дата рожд 57, дата письма 55, договор 34,
период 41/44, срок действия 16, docType 61/61. needsReview 12 (с понятными подсказками).
**Влияет на:** `lib/db/schema/guarantee.ts` (+поля), `enums.ts` (docType/reviewStatus/annul), `parse-log.ts`
(миграции 0005–0008), `app/(admin)/parse-log`, `components/admin/shell.tsx` (nav), `api/registry/export`,
`lib/server/registry/queries.ts`, `lib/review-hints.ts`, `lib/letter-status.ts`, `.mail-intake/{extract_dataset,enrich}.py`.

## [2026-07-22] D16. S1 — живой авто-приём: серверный python-раннер + инкрементальный upsert (не порт в Node)
**Контекст:** боевые ящики подключены (read-only, app-пароли). Нужен непрерывный приём: письмо пришло →
скачалось → распозналось парсерами → в реестр. Рабочая распознавалка — оффлайн python (`.mail-intake/*.py`),
её тулзам (poppler, xlrd/striprtf/olefile, LLM) нужен рантайм; батч-сид (`corpus.ts`) делает full wipe+reinsert.
**Решение (проверенные гипотезы):**
- **НЕ переписываем распознавание в Node.** Reuse рабочего python как **серверного раннера** на systemd-таймере
  (3 мин): `fetch_live.py inc` → `extract`+`enrich` → `db:ingest`. Порт в Node = дублирование + дрейф (отклонили).
- **Инкрементальность по UID**, не по дате: курсор `live/state.json` `{box: lastUid}`, `UID last+1:*` (read-only,
  BODY.PEEK). Бутстрап = текущий максимум (приём только НОВЫХ; история — бэкофиллом). Гэпов/дублей нет.
- **Отдельный `ingest.ts` (upsert, БЕЗ wipe)** vs `corpus.ts` (wipe): живой приём не должен стирать реестр и ручные
  правки. Дедуп по `rawSha256` письма (не по Message-ID — устойчивее). Общие нормализаторы вынесены в `shared.ts`
  (один источник для corpus/ingest, без дублирования). `parse_log` — вставка чанками (лимит параметров PG 65535).
- **Self-healing:** письмо известного домена, но НОВОГО типа/шаблона → запись всё равно в реестре, `doc_template`
  авто-создаётся (`status=new`, note), запись форсит `needsReview`, админу — `error_report(open)` на первой записи.
- **Инфра:** poppler-utils + pip(xlrd,striprtf,olefile) на хосте (LibreOffice НЕ нужен — xlsx pure-python zipfile).
  Раннер `/opt/sib-intake/`, creds `.env.local`(600), units `sib-intake.{service,timer}`, лог `intake.log`(ротация 5MB).
**Валидация:** реальный цикл — fetch 31 → extract 61 → enrich (LLM 11) → ingest 3 новых + 28 дедуп; таймер авто-сработал (success).
**Влияет на:** `lib/db/seed/{ingest,shared,corpus}.ts`, `package.json` (`db:ingest`), `.mail-intake/fetch_live.py`
(режим `inc`/`--bootstrap`), сервер `/opt/sib-intake/*` + systemd. Read-only почты (guardrails) соблюдён.

## [2026-07-23] D17. Дата письма ≠ дата договора ≠ «Получено»; колонка «Получено» (пояс партнёра); backfill existing
**Контекст:** владелец: у СОГАЗ `letterDate` неверна — парсер брал дату ДОГОВОРА. Пример (Якуткин): в теле
`Приложение к договору № 0823RP293 от 01.01.2024` (дата договора) и `Исх.№ 08ПП0011107 \n 21.07.2026` (реальная
дата письма). Слабая метка `(?:исх|гп|№)…от` ловила «договору № … от 01.01.2024» и перекрывала верную дату.
**Решение (проверено на 9660 реальных писем локально):**
- **letterDate = дата ПИСЬМА из содержимого**, не первая дата и не дата договора/приложения/полиса/получения.
  Метки разделены на СИЛЬНЫЕ (`дата письма`, `письмо от`, `дата выдачи/документа`, `дата:`) и СЛАБЫЕ (`№…от`);
  слабые применяются с `deny_before` (отсев `договор|приложени|полис|соглашени` в 30 симв. перед меткой).
- **Дата у «Исх.№…»**: СОГАЗ — строка-дата ПОД номером (`IZH_AFTER`), Росгосстрах — НАД (`IZH_BEFORE`). Для
  СОГАЗ-реестров дата письма часто в XLSX-вложении (тело пустое) — берётся из `combined` (тело+вложения).
- **«Получено» — ОТДЕЛЬНАЯ колонка** (после «Дата письма») = `emailMessage.receivedAt`, у ВСЕХ строк. Пишем как
  настоящую Excel-datetime в поясе ПАРТНЁРА «Клиника Сибирская» — **Томск GMT+7** (без DST). Серийник Excel
  считаем детерминированно (`25569 + (utc+7ч)/сут`, округление до секунд) — не зависит от TZ сервера/версии exceljs.
- **ID записи — первая колонка Excel** (`guarantee_letter.id`) — чтобы владелец сообщал о косяках по ИД.
- **Backfill existing (важно): `db:ingest` = INSERT-only с дедупом по `rawSha256`** — повторный ingest НЕ обновляет
  уже сохранённые записи. Для правки истории — прямой SQL. Безопасное правило: менять `letter_date` ТОЛЬКО там, где
  текущее значение = старому детерминированному (LLM не переопределял) и новое даёт конкретную дату; строка меняется
  лишь если её текущая дата совпадает со старой ошибочной. Снапшот старых значений → CSV на сервере (откат).
**Применено на проде (2026-07-23):** backfill 376 писем / **3426 записей** (СОГАЗ 2840: `01.01.2024`→реальная;
Росгосстрах 586: `2019`→дата над «Исх.»); +4 записи живого приёма (dms-2900/01/02) переразобраны из .eml (тело+xlsx).
Остаток `letter_date='2024-01-01'` = 0. Якуткин: `2024-01-01`→`2026-07-21`. Проверка 5 страховых: у Альфа/Энергогарант
даты письма в теле/файле РЕАЛЬНО НЕТ (только период покрытия/субъект) — оставлено пусто/LLM; РЕСО/Лучшее — есть, ловится.
**Влияет на:** `.mail-intake/extract_dataset.py` (`letter_date`, `date_near(deny_before)`, `IZH_*`; локально И
`/opt/sib-intake/.mail-intake/`), `app/api/registry/export/route.ts` (колонки ID/Получено), прод-БД `guarantee_letter.letter_date`.

## [2026-07-23] D18. field_status — «нет данных» ≠ «не распознано» (пустота типизирована)
**Решение:** `guarantee_letter.field_status` jsonb (миграция 0014): per-field статус `found|absent|unreadable`.
`absent` = искали в ФАЙЛЕ и в ТЕЛЕ — поля в источнике нет → UI/Excel показывают «нет данных»; `unreadable` =
вложения есть, но текст не извлёкся (пароль/скан/нет библиотеки) → «не распознано» (+needsReview-контур);
без статуса → пусто (совместимость). Маркер в само поле НЕ пишем — только в статус. Рендер: `cellText`
(`lib/letter-status.ts`) в Excel-экспорте и карточке. Эмиссия: парсер (`fields_file_first`, реестровые строки)
проставляет статусы, `enrich.prune_fs` снимает статус с полей, дозаполненных LLM. Backfill прода: caseNumber
8367 (enroll/detach/annul — в списках № обращения структурно нет), letterDate 604 (Альфа/Энергогарант — в
файле даты письма нет, D17).
**Почему:** правило владельца — человек должен видеть «реально нет данных», а не гадать «пусто = сбой?».
**Альтернативы:** маркер-строка в поле (засоряет данные), отдельные nullable-колонки (взрыв схемы) — отвергнуты.
**Влияет на:** schema/guarantee, seed shared/ingest/corpus, queries, export, карточка, парсер+enrich (обе копии).

## [2026-07-23] D19. Файл-first в свободном пути парсера + честный `source` + сила извлечения
**Решение:** свободный (не-табличный) путь разбора теперь двухпроходный `fields_file_first`: поля ищутся
СНАЧАЛА в текстах ВЛОЖЕНИЙ, тело+тема — фолбэк (раньше единый блоб «тема→тело→вложения» давал телу приоритет).
Исключение — ФИО: **сила извлечения важнее источника** (метка «Пациент:/на имя» из тела бьёт слабый
FIO2-фолбэк из файла — кейс Балта). `source` честный: тег вложения только если хоть одно поле взято из файла.
mech_ok — строго по-старому (пасс-уровень), чтобы не сузить LLM-кросс-чек. Регресс-контур: корпусный прогон
до/после + diff по полям (проверены Балта/Зетта/СОГАЗ/Совкомбанк по файлам). Плюс: `status_of` травился
шапкой «Дата открепления» в файлах ПРИКРЕПЛЕНИЯ → однозначное ИМЯ ФАЙЛА (прикреп/prikr ↔ откреп/снят/snyat)
приоритетнее blob-keywords; серийники-даты не клеятся в полис (РЕСО 414-симв. мусор); чисто-числовые ячейки
не попадают в услуги («1.0/2.0» Энергогарант).
**Backfill прода:** Ингосстрах ПРИКР→enroll **896**, Ренессанс откр→detach **53**, РЕСО полис=1-й токен (3),
services без числового мусора (193). Снапшот `_bak_v2_batch_20260723` (1226 строк).
**Почему:** доктрина владельца «файл первичен»; повторный аудит вскрыл перевёрнутый смысл прикреплений.
**Влияет на:** `.mail-intake/extract_dataset.py`+`enrich.py` (локально и `/opt/sib-intake`), прод-данные.

## [2026-07-23] D20. Дубли — пометка на приёме и в реестре (реализация D6)
**Решение:** `guarantee_letter.is_duplicate` + `duplicate_of_id` (миграция 0015). Ключ дубля:
страховая+ФИО(нормализовано)+полис+тип+дата письма (`dupKey` в `lib/db/seed/shared.ts`); неполный ключ (нет
пациента/полиса/типа) дублем НЕ считается. Ingest держит канонический индекс по клинике и помечает повторы
на приёме; corpus — внутрибатчево. UI: значок ⧉ в реестре, ссылка «открыть первую запись» в карточке,
колонка «Дубль» в конце Excel (позиции прежних колонок не сдвигаются). Повторы НЕ удаляем и НЕ скрываем.
**Факт:** Ингосстрах шлёт один список многократно с разбегом ~16 минут — 1772 группы/3581 строка дублей
на 2026-07-23 (v2-аудит); классический кейс — двойная отправка в тот же ящик, не кросс-ящик.
**Альтернативы:** авто-удаление (запрещено D6), дедуп по sha письма (не ловит пере-отправку с новым
Message-ID) — отвергнуты.
**Влияет на:** schema, ingest/corpus, queries, реестр/карточка/экспорт, прод-backfill детекции.

## [2026-07-23] D21. Пер-страховые шаблонные правила отдельным модулем + валидация по «золотому корпусу»
**Решение:** шаблонные ГП/направления 6 страховых (Росгосстрах, Совкомбанк, ВСК, СОГАЗ-направления,
Зетта, Лучи) разбираются детерминированными правилами в **отдельном модуле `.mail-intake/rules_gp.py`**
(самодостаточный — без импорта из extract_dataset, чтобы валидатор грузил его standalone). Интеграция:
`fields_from_text` ДОЗАПОЛНЯЕТ правилами только пустые поля (generic не перебивается); `mech_ok` =
надёжное ФИО (метка ИЛИ нумерованный реестр) + полис/№ГП → полный LLM-проход не нужен, LLM — подстраховка
пробелов (D15). **Процесс валидации:** новые правила обязаны пройти офлайн-сверку по «золотому корпусу» —
историческим LLM-значениям в прод-БД на сохранённых файлах (`validate_rules.py`): agreement по policy/
validUntil 100%, СОГАЗ coverage 95% (2 раунда итераций; маска ВСК расширена до суффикса `-\d{2,6}` — узкая
резала хвост). **Семантика coverage:** правила кладут период ПОЛИСА (по схеме), LLM исторически клала период
ГП (дублируя validUntil) — историю НЕ бэкфиллим, mismatch этого класса — не ошибка правил.
**Почему:** 724 записи (7.8%) шли через LLM, при этом ~76% исходников — жёсткие бланки; детерминизм
дешевле, воспроизводим и проверяем по золоту. **LLM осознанно остаётся:** сканы (vision), РЕСО-тела
(свободный текст оператора), ЭГ-направления (.doc — до появления catdoc/antiword в раннере), новые формы.
**Альтернативы:** правила внутри extract_dataset (циклический импорт с валидатором), LLM-даунгрейд модели
(не решает воспроизводимость) — отвергнуты.
**Влияет на:** `.mail-intake/rules_gp.py` (новый), `extract_dataset.py` (apply_rules, mech_ok, POLIS_MASKS
ВСК), обе копии (локально + `/opt/sib-intake`), процесс добавления правил (валидация по золоту обязательна),
`plans/llm-to-deterministic.md`.
