# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-07-26 — в рабочем дереве 3 незакоммиченных файла экспорта реестра (route/dialog/queries: «Дентал Про» фолбэк программ) — работа ПАРАЛЛЕЛЬНОЙ сессии, моя сессия их не трогает и не коммитит; её гейт закроет её же коммит. Похоже, подключается второй клиент «Дентал Про» — при подтверждении завести в память (онбординг-плейбук guides/onboarding.md).
- 2026-07-26: e2e ЛОКАЛЬНО сломаны обновлением ОС (ядро 7.0.0-27/glibc 2.43): chromium int3-трапы + ERR_INSUFFICIENT_RESOURCES; firefox-докачка заблокирована сетью. Решение: e2e на прод-сервере /root/sib-e2e (rsync→pnpm i→build→postgres:16 контейнер sib-e2e-db:5436→playwright). В playwright.config добавлен --no-sandbox.
- 2026-07-26: списки страховых: срок+программа живут в ПРЕАМБУЛЕ (Ренессанс «на срок:», РГС «Программа по факту:»), РЕСО даты в колонке «Открепление с» серийниками; открепления без даты = дата письма; СОГАЗ «Вид изменяемых данных» — мусор → other (99 файлов, 575 записей).
- 2026-07-26: чат по правилам: coverage_chat_message (миграция 0025), история общая на пациента, деткий фолбэк без LLM; GENERIC_WORDS фикс релевантности.
- 2026-07-26: тарифы EN: $150/250/400/500 реестр, ИИ те же сверху (владелец). Лендинг v3.1: демо-баннер, без блока цифр, «Секунды вместо часов», «часто дороже», контакты 1 раз.
