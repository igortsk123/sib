---
tier: 2
topic: deployment
scope: Playbook деплоя/окружения — заготовка (greenfield, прод ещё нет)
tier1: ""
updated: 2026-06-21
importance: med
source: manual
status: working
---

# Deployment — playbook (заготовка)

> Прод ещё НЕ развёрнут (greenfield). Где хранить ПДн и кто оператор данных — решение владельца
> (`core/human-decisions.md`, ADR D4). Наполнить при первом деплое.

## Решения владельца до прода (блокеры)
- Где сервер: клиника / РФ-облако / VPS / закрытый контур (ПДн!). Шифрование диска. Бэкапы. Сроки хранения.

## Целевой пайплайн (когда появится)
1. Trigger: push в `main` / тег. 2. Гейт: typecheck+lint+unit+integration+e2e+build (красный → не деплоим).
3. Сборка артефакта; предыдущий — как `:prev`. 4. Деплой. 5. **Smoke** (health + ключевой поток).
6. Провал smoke → **авто-откат** на `:prev`.

## Сеть безопасности (обязательна для ПДн)
- Health-эндпоинт; бэкап + **restore-drill**; миграции expand-only (откат образа безопасен); бэкап перед
  опасными операциями над данными. Подход — `quality-criteria.md`.

## Окружение
- Секреты — в менеджере секретов/CI, не в памяти. Локально — `.env.local` (gitignored).
- Внешний доступ: Яндекс IMAP/SMTP, OpenAI-прокси (РФ→403 без прокси), Inngest.

## Что НЕ ломать (наполнить при проде)
- (перечислить другие сервисы/контейнеры на хосте, делящие ресурсы).
