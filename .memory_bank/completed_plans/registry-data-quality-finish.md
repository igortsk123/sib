---
tier: 2
topic: plan-registry-data-quality-finish
scope: Доведение очереди владельца по качеству данных реестра — фабрикация дат, срок у аннулирований, акты проверки
status: completed
source: manual
updated: 2026-08-11
importance: high
completed: 2026-08-11
---

# План: закрыть остаток очереди по качеству реестра (из `registry-data-quality-audit.md`)

## Почему сейчас
Очередь поставлена владельцем 23.07 и с тех пор не двигалась. Это данные, которые видит клиника, —
цена ошибки выше, чем у остальных хвостов. Проверка на проде (11.08): 13 978 записей.

## Что проверено на реальных данных (11.08)
- **ДР на 01.01 — 43 записи** из 13 759 с заполненной датой. Это ~0,31% при естественной доле
  1/365 ≈ 0,27% → тотальной фабрикации НЕТ, но каждую надо сверить с исходником.
- **letter_date на 01.01 — 0 записей.** Проблемы нет, пункт закрывается проверкой.
- **annul — 48 записей, из них 22 со сроком действия/периодом.** Правило владельца: аннулирование
  отменяет ГП, срок действия у него не атрибут → чистить.
- Текст исходников в БД не хранится (0 из 2818 вложений) → сверка только по файлам `/opt/sib-storage`.

## Задачи
- [x] Правило классификатора: «акт (сверки|проверки|экспертизы|осмотра)» → `docType=other`
      (`extract_dataset.doc_type_of`) + скан прода и backfill.
- [x] `docType=annul` → `validUntil`/`coverageFrom`/`coverageTo` не заполнять (в извлечении) +
      очистка 22 записей.
- [x] ДР 01.01: сверка каждой из 43 записей с текстом исходника; день/месяц не подтверждён → `null`
      + пометка. Записи из xlsx-ячеек (текстом не проверяются) не трогаем вслепую.
- [x] Скрипт `.mail-intake/data_quality_fix.py` (dry по умолчанию, `--apply`), TSV-бэкап прежних
      значений перед изменением — откат возможен.
- [x] Гейт + деплой правил в интейк (`/opt/sib-intake`), память.

## Критерии приёмки
- Ни одной записи `docType=annul` со сроком действия/периодом.
- Каждая оставшаяся ДР на 01.01 подтверждена текстом исходника (или помечена как непроверяемая).
- Новые письма-акты классифицируются как `other` (регресс-проверка правила на боевом тексте).
- Гейт зелёный, `/memory-check` чисто, бэкап изменений лежит на сервере.

## Definition of Done — память
- [x] ADR/обновление `registry-data-quality-audit.md` (что закрыто, что осталось)
- [x] `/memory-check` выполнен, audit «чисто»

## Итог (2026-08-11, применено на проде)
- **Аннулирования:** 22 записи со сроком действия/периодом → очищены, стало **0**. В извлечении
  (`extract_dataset.fields_from_text`) для `docType=annul` поля `validUntil/coverageFrom/coverageTo`
  теперь принудительно пустые — LLM-лотерея на серийных аннулированиях закрыта в корне.
- **Акты:** правило `ACT` («акт …сверки|проверки|экспертизы|осмотра|разногласий», допускается 2 слова
  между) → `docType=other`, ищется в первых 600 символах, чтобы не ловить «акт выполненных работ»
  внутри обычного ГП. Самопроверка на 8 формулировках чистая. На проде подходящих записей 0 —
  единственный известный случай (1caa2c61) владелец уже перевёл руками, правило закрывает будущие.
- **Даты рождения 01.01:** 43 записи. Сверка с исходником (текст письма/вложения, а для реестров —
  ячейка Excel с разворотом серийника в дату): **34 подтверждены**, **5 — фабрикация** (в источнике
  только год) → снято в null + пометка, **4 не проверяются** (не тронуты). Осталось 38 при
  естественном ожидании ~38 (1/365 от 13 759) — массовой фабрикации нет, пункт закрыт.
- **letter_date на 01.01: 0 записей** — проблемы не было.
- Скрипты интейка в git НЕ версионируются (`.mail-intake` в .gitignore — рядом лежат ПДн):
  канон — `/opt/sib-intake/.mail-intake/` на сервере, локальная копия синхронизируется rsync.
- Бэкапы прежних значений: `/opt/sib-intake/backups/annul_dates_*.tsv`, `birthdate_0101_*.tsv`.

## Уроки
- Проверять гипотезу о «фабрикации» надо базовой долей: 43 записи из 13 759 — это ~0,31% при
  естественных 0,27%. Без этой прикидки легко снести 38 настоящих дат рождения 1 января.
- В xlsx даты лежат ЧИСЛОМ-серийником, поэтому текстовый поиск «года» по листу находит пустоту.
  Разворот серийника в дату превратил 34 «непроверяемых» записи в подтверждённые.
- `row_index` записи мог сместиться относительно текущего разбора листа → сверять сначала по строке,
  затем по всему листу, иначе получаем ложное «не проверить».
